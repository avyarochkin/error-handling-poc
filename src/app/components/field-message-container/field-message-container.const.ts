import { ValidationErrors } from '@angular/forms';

export const validatorErrors: ValidationErrors = {
    required: 'Value is required',
    pattern: 'Value does not match expected pattern',
    email: 'Wrong email format'
}
